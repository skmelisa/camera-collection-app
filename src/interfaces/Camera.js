export default class Camera {
  constructor(id, brand, model, year, imageUrl, status) {
    this.id = id;
    this.brand = brand;
    this.model = model;
    this.year = year;
    this.imageUrl = imageUrl;
    this.status = status; 
  }
}